{
  "name": "latin-syntax-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "secret": "wrangler secret put OPENAI_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
